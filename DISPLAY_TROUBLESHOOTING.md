# 🔍 顯示問題排查指南

## 問題描述
用戶反映「沒有顯示」補庫存和調整上限功能。

## 功能位置

### 補庫存和調整上限欄位
**位置**: 後台管理 → 獎品管理 → 點擊「編輯」按鈕

**顯示條件**:
- ✅ 必須在編輯模式下（`editingPrize` 不為 null）
- ✅ 必須打開獎品編輯彈窗（`showPrizeModal` 為 true）

## 檢查步驟

### 1. 確認是否在編輯模式
- 必須點擊獎品的「編輯」按鈕
- 編輯彈窗應該會打開
- 在彈窗中應該能看到「補庫存數量」和「調整總數量上限」欄位

### 2. 檢查服務器狀態
```bash
# 確認服務器運行在正確的端口
curl http://localhost:3000/api/prizes
```

### 3. 檢查瀏覽器控制台
打開瀏覽器開發者工具（F12），查看：
- Console 標籤是否有錯誤
- Network 標籤確認 API 請求是否成功

### 4. 強制刷新頁面
- 按 `Ctrl+Shift+R` (Windows) 或 `Cmd+Shift+R` (Mac)
- 或清除瀏覽器緩存後重新訪問

## 功能顯示位置

### 編輯表單中的欄位順序
1. 獎品名稱 *
2. 總數量 *（編輯模式下禁用）
3. **補庫存數量（可選）** ← 只在編輯模式顯示
4. **調整總數量上限（可選）** ← 只在編輯模式顯示
5. 獎品圖片
6. 結束語

## 代碼確認

### 條件渲染
```typescript
{editingPrize && (
  <>
    <div>
      <label>補庫存數量（可選）</label>
      {/* ... */}
    </div>
    <div>
      <label>調整總數量上限（可選）</label>
      {/* ... */}
    </div>
  </>
)}
```

### 編輯按鈕邏輯
```typescript
onClick={() => {
  setEditingPrize({ ... }) // 設置編輯狀態
  setNewPrize({ ... })     // 設置表單數據
  setShowPrizeModal(true)  // 打開彈窗
}}
```

## 可能的原因

### 1. 沒有點擊「編輯」按鈕
- 這些欄位只在編輯模式下顯示
- 必須先點擊獎品的「編輯」按鈕

### 2. 服務器未正確啟動
- 確認服務器運行在端口 3000
- 檢查是否有編譯錯誤

### 3. 瀏覽器緩存問題
- 強制刷新頁面
- 清除瀏覽器緩存

### 4. React 狀態問題
- 檢查 `editingPrize` 是否正確設置
- 檢查 `showPrizeModal` 是否為 true

## 測試步驟

1. **訪問後台管理**
   - URL: `http://localhost:3000/admin/attendance_management`
   - 切換到「獎品管理」標籤

2. **點擊編輯按鈕**
   - 找到任意一個獎品
   - 點擊「編輯」按鈕

3. **檢查彈窗內容**
   - 應該看到「補庫存數量（可選）」欄位
   - 應該看到「調整總數量上限（可選）」欄位
   - 應該看到「當前總數量：X」的提示

4. **如果仍然沒有顯示**
   - 打開瀏覽器開發者工具（F12）
   - 查看 Console 標籤是否有錯誤
   - 查看 Network 標籤確認請求是否成功

---

**排查時間**: 2026-01-13  
**狀態**: 代碼正確，需要確認用戶操作步驟
