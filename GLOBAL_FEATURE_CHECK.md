# 全域功能檢查報告

檢查日期：依執行日
檢查項目：同步與資料同步、獎品相關簽到名單、按鈕功能

---

## 1. 同步與資料同步

### 1.1 資料庫同步（單一來源）

| 項目 | 狀態 | 說明 |
|------|------|------|
| 出席資料庫 | ✅ | 全部使用 `estate_attendance_*` 表，無個別資料庫 |
| API 寫入 | ✅ | 所有 API 使用 `supabaseService` 寫入同一資料庫 |
| 統計報表 | ✅ | 以「有簽到的會議」為基準，避免空會議拉低出席率 |

### 1.2 Google Sheets 同步

| 項目 | 狀態 | 說明 |
|------|------|------|
| 同步方向 | ✅ | 單向：資料庫 → Google Sheets |
| 同步時機 | ✅ | 會員新增/編輯/刪除時背景同步；手動「📊 同步到 Sheets」按鈕 |
| 同步內容 | ✅ | 僅會員資料（id, name, profession） |
| 前置條件 | ⚠️ | 需設定 `GOOGLE_SHEETS_SPREADSHEET_ID` 等環境變數 |

### 1.3 資料寫入來源對照

| 功能 | API/腳本 | 目標表 |
|------|----------|--------|
| 出席管理 | `/api/checkin`, `/api/checkin/delete` | checkins |
| 會議管理 | `/api/meetings`, `/api/meetings/[id]` | meetings, checkins |
| 會員管理 | `/api/members/create`, `/api/members/[id]` | members |
| 統計匯入 | `/api/statistics/import` | meetings, checkins |
| 出席 CSV | `scripts/import-attendance-csv.mjs` | members, meetings, checkins |
| 備份還原 | `/api/backup/restore` | members, meetings, prizes, checkins, winners |

---

## 2. 獎品相關簽到名單導入

### 2.1 抽獎資格（已修正）

| 項目 | 修正前 | 修正後 |
|------|--------|--------|
| 可抽獎狀態 | 僅 `present` | `present`, `early`, `late`, `early_leave`, `proxy` |
| 抽獎 API | 使用 `supabase` (anon) | 使用 `supabaseService` |
| 抽獎頁面名單 | 僅顯示 status=present | 顯示所有出席狀態 |

### 2.2 簽到資料流

- **簽到頁** `/checkin`：會員簽到 → `POST /api/checkin` → `estate_attendance_checkins`
- **出席管理**：手動簽到/儲存/刪除 → 同上
- **抽獎頁** `/lottery`：讀取 `/api/checkins?date=xxx` 取得該日簽到者，作為抽獎候選名單
- **抽獎**：`POST /api/lottery/draw` 從候選名單隨機選出中獎者

### 2.3 獎品管理

- 獎品 CRUD：`/api/prizes`, `/api/prizes/[id]`
- 圖片上傳：Supabase Storage
- 抽獎時自動扣除獎品剩餘數量

---

## 3. 按鈕功能檢查

### 3.1 出席管理標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 批量簽到 | 為選中會員簽到 | ✅ |
| 批量刪除 | 刪除選中會員的簽到 | ✅ |
| 儲存（每列） | 儲存該列簽到/出席狀態/留言 | ✅ |
| 刪除（每列） | 刪除該列簽到 | ✅ |
| 篩選：全部/已簽到/遲到/早退/代理/缺席 | 篩選列表 | ✅ |
| 排序 | 依編號/姓名/時間/狀態 | ✅ |

### 3.2 會員管理標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 匯入會員 CSV | 從 CSV 新增會員 | ✅ |
| 📊 同步到 Sheets | 同步會員至 Google Sheets | ✅ |
| 編輯/刪除會員 | 會員 CRUD | ✅ |

### 3.3 會議管理標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 新增會議 | 建立新會議 | ✅ |
| 查看/編輯/刪除 | 會議 CRUD | ✅ |

### 3.4 統計報表標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 匯入 CSV | 匯入統計 CSV 並重建簽到 | ✅ |
| 分析 CSV | 分析 CSV 與現有資料差異 | ✅ |
| 排序選項 | 依編號/姓名/專業別/各統計欄位 | ✅ |

### 3.5 獎品管理標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 新增獎品 | 新增獎品（含圖片） | ✅ |
| 編輯/刪除獎品 | 獎品 CRUD | ✅ |
| 前往抽獎轉盤 | 連結至 /lottery | ✅ |

### 3.6 系統設定標籤

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 🔍 檢查資料庫 | 檢查資料表是否存在 | ✅ 已新增 |
| 📋 建立資料表 | 建立/重置資料表 | ✅ 已新增 |
| 💾 備份資料庫 | 下載 JSON 備份 | ✅ 已強化（含獎品） |
| 📥 還原資料庫 | 上傳備份並還原 | ✅ 已修正（呼叫 API） |
| 🗑️ 清除所有簽到記錄 | 刪除所有簽到 | ✅ |
| 儲存設定 | 儲存系統參數至 localStorage | ✅ |

### 3.7 抽獎頁面

| 按鈕 | 功能 | 狀態 |
|------|------|------|
| 開始抽獎 | 執行抽獎 | ✅ |
| 刪除中獎記錄 | 刪除指定中獎記錄 | ✅ |
| 刷新 | 重新載入資料 | ✅ |

---

## 4. 本次修正摘要

1. **抽獎資格**：遲到、早退、代理出席者納入可抽獎名單
2. **抽獎 API**：改用 `supabaseService`，並支援所有出席狀態
3. **資料還原**：還原按鈕改為呼叫 `/api/backup/restore`
4. **資料備份**：備份內容加入獎品
5. **檢查/建立資料庫**：新增「檢查資料庫」「建立資料表」按鈕
6. **介面文字**：資料庫顯示改為 Supabase PostgreSQL
